# Core dependencies
qdrant-client==1.9.0
redis==5.0.0
FalkorDB>=1.0.0
requests==2.31.0
python-dotenv==1.0.0

# Reranker (optional — system degrades gracefully without it)
fastapi==0.110.0
uvicorn==0.29.0
pydantic==2.12.5
transformers>=4.40.0
torch>=2.0.0
sentencepiece>=0.2.0

# STORM Wiki generator (optional — generates wiki articles from memory)
knowledge-storm>=0.2.0
dspy-ai>=2.4.0

# Memory engine extras
numpy>=1.24.0
sentence-transformers>=2.2.0

# MCP server (optional — for memory-mcp-server.py)
mcp>=1.0.0
